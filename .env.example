# ═══════════════════════════════════════════════════════════════
# cert-parser — Environment Configuration
# ═══════════════════════════════════════════════════════════════
# Copy this file to .env and fill in the actual values.
# NEVER commit .env — it contains secrets.
# In Kubernetes, these are overridden by ConfigMap/Secret.

# ── Step 1: OpenID Connect Authentication ────────────────────
# Token endpoint URL (Keycloak / any OIDC provider)
AUTH_URL=https://keycloak.example.com/realms/app/protocol/openid-connect/token
AUTH_CLIENT_ID=your-client-id
AUTH_CLIENT_SECRET=your-client-secret
AUTH_USERNAME=your-username
AUTH_PASSWORD=your-password

# ── Step 2: SFC Login ────────────────────────────────────────
# Login endpoint URL
LOGIN_URL=https://api.example.com/auth/v1/login
# Border post configuration (integers)
LOGIN_BORDER_POST_ID=1
LOGIN_BOX_ID=1
LOGIN_PASSENGER_CONTROL_TYPE=1

# ── Step 3: Certificate Download ─────────────────────────────
DOWNLOAD_URL=https://api.example.com/certificates/csca

# ── HTTP Settings ────────────────────────────────────────────
# Timeout in seconds for all HTTP requests
HTTP_TIMEOUT_SECONDS=60

# ── PostgreSQL ──────────────────────────────────────────────
DATABASE_DSN=postgresql://user:password@localhost:5432/cert_parser_db

# ── Scheduler ──────────────────────────────────────────────
# Interval in hours between scheduled runs
SCHEDULER_INTERVAL_HOURS=6
# If true, run once immediately on startup before starting scheduler
RUN_ON_STARTUP=true

# ── Logging ──────────────────────────────────────────────
LOG_LEVEL=INFO
