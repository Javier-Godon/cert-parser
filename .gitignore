# Environment / secrets — NEVER commit
.env
.env.*
!.env.example

# Dagger pipeline credentials — NEVER commit
dagger_go/credentials/

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/
*.egg
.eggs/

# Virtual environments
.venv/
venv/
env/

# IDEs
.idea/
.vscode/settings.json
*.swp
*.swo

# Testing
.coverage
htmlcov/
.pytest_cache/
.mypy_cache/

# OS
.DS_Store
Thumbs.db

# Downloaded binaries (re-downloadable)
data/*.bin
data/*.ml
data/*.der

# ICAO PKD confidential resources — NEVER commit (large LDIF dumps, certificates)
src/cert_parser/resources/confidential/

# ICAO PKD LDIF resources (large files, re-downloadable from ICAO)
src/cert_parser/resources/**/*.ldif

# Test fixtures (generated from LDIF via scripts/extract_ldif_fixtures.py)
tests/fixtures/*.bin
tests/fixtures/*.der
tests/fixtures/*.pem
tests/fixtures/*.crt
tests/fixtures/*.key
!tests/fixtures/README.md

# Certificates and private keys — NEVER commit
*.p12
*.pfx
*.key
*.pem
*.csr
